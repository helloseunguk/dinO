"""
보스 설정 상수 모음 (오딘: 발할라 라이징)
탭별 → 게임 UI 표시 순서대로 정리
"""

from datetime import timedelta

from models import BossConfig

_ALERT = timedelta(minutes=2)

# 체크 간격 (초)
CHECK_INTERVAL = 10
# 동기화 간격 (초)
SYNC_INTERVAL = 59 * 60  # 59분 — drift 누적 최소화 위해 24h → 59m

def _boss(name: str, hours: int, region: str = "") -> BossConfig:
    return BossConfig(name=name, respawn_interval=timedelta(hours=hours), region=region, alert_before=_ALERT)


def _boss_min(name: str, minutes: int, region: str = "",
              alert: timedelta = _ALERT) -> BossConfig:
    """리젠 주기가 분 단위인 보스 (테스트용)."""
    return BossConfig(name=name, respawn_interval=timedelta(minutes=minutes),
                      region=region, alert_before=alert)


# === 요툰하임 ===
요툰하임 = [
    _boss("파르바", 12, "요툰"),
    _boss("흐니르", 12, "요툰"),
    _boss("바우티", 12, "요툰"),
    _boss("야른", 12, "요툰"),
    _boss("셀로비아", 12, "요툰"),
    _boss("페티", 12, "요툰"),
    _boss("니드호그", 12, "요툰"),
    _boss("티르", 72, "요툰"),
]

# === 니다벨리르 ===
니다벨리르 = [
    _boss("라이노르", 12, "니다벨"),
    _boss("헤르모드", 12, "니다벨"),
    _boss("브륀힐드", 12, "니다벨"),
    _boss("수드리", 12, "니다벨"),
    _boss("비요른", 12, "니다벨"),
    _boss("스칼라니르", 12, "니다벨"),
    _boss("라타토스크", 12, "니다벨"),
    _boss("토르", 72, "니다벨"),
]

# === 알브하임 ===
알브하임 = [
    _boss("스바르트", 24, "알브"),
    _boss("모네가름", 24, "알브"),
    _boss("굴베이그", 24, "알브"),
    _boss("두라스로르", 24, "알브"),
    _boss("드라우그", 24, "알브"),
    _boss("오딘", 72, "알브"),
]

# === 무스펠하임 ===
무스펠하임 = [
    _boss("메기르", 36, "무스펠"),
    _boss("헤르가름", 36, "무스펠"),
    _boss("엘드룬", 36, "무스펠"),
    _boss("수르트", 72, "무스펠"),
    _boss("신마라", 36, "무스펠"),
    _boss("탕그리스니르", 36, "무스펠"),
    _boss("우로보로스", 36, "무스펠"),
]

# === 아스가르드 ===
아스가르드 = [
    _boss("발리", 48, "아스"),
    _boss("샤무크", 48, "아스"),
    _boss("화신 그로아", 48, "아스"),
    _boss("노트", 48, "아스"),
    _boss("스칼드메르", 48, "아스"),
    _boss("미미르", 72, "아스"),
]

# === 니플하임 ===
니플하임 = [
    _boss("히로킨", 60, "니플"),
    _boss("헤이드", 72, "니플"),
    _boss("이미르", 72, "니플"),
    _boss("호드", 72, "니플"),
    _boss("대교주 프레이", 72, "니플"),
]

# === 던전 ===
던전 = [
    _boss("혼돈의 마수 굴베이그", 12, "던전"),
    _boss("혼돈의 참수자 스네르", 24, "던전"),
    _boss("나테의 드라우그", 36, "던전"),
    _boss("혼돈의 사제 강글로티", 36, "던전"),
    _boss("분노의 모네가름", 36, "던전"),
    _boss("기만의 기사 다인홀로크", 72, "던전"),
]

# === 미드가르드 (테스트용 — 리젠 10분) ===
미드가르드 = [
    _boss_min("그로아(미드)", 10, "미드"),
    _boss_min("매트리악", 10, "미드"),
    _boss_min("탕그뇨스트", 10, "미드"),
    _boss_min("칼바람 하피", 10, "미드"),
    _boss_min("레라드", 10, "미드"),
]

# 탭별 보스 목록
TAB_BOSS_MAP: dict[str, list[BossConfig]] = {
    "요툰하임": 요툰하임,
    "니다벨리르": 니다벨리르,
    "알브하임": 알브하임,
    "무스펠하임": 무스펠하임,
    "아스가르드": 아스가르드,
    "니플하임": 니플하임,
    "던전": 던전,
    # "미드가르드": 미드가르드,   # 비활성화 (테스트 종료)
}

# 전체 보스 리스트 (다른 모듈 호환용)
BOSS_CONFIGS: list[BossConfig] = []
for tab_bosses in TAB_BOSS_MAP.values():
    BOSS_CONFIGS.extend(tab_bosses)

# === 탭별 보스 남은 시간 OCR 좌표 (게임 내 좌표) ===
# 각 탭의 보스 순서에 맞춰 시간 텍스트 중심 좌표를 입력
# TODO: 유저가 좌표 입력
TAB_TIME_COORDS: dict[str, list[tuple[int, int]]] = {
    "요툰하임":   [(687,532), (952,532), (1217,532), (1482,532), (687,752), (952,752), (1217,752), (1482,752)],  # 8개 보스 좌표
    "니다벨리르": [(687,532), (952,532), (1217,532), (1482,532), (687,752), (952,752), (1217,752), (1482,752)],  # 8개 보스 좌표
    "알브하임":   [(687,532), (952,532), (1217,532), (687,752), (952,752), (1217,752)],  # 6개 보스 좌표
    "무스펠하임": [(687,532), (952,532), (1217,532), (1482,532), (687,752), (952,752), (1217,752)],  # 7개 보스 좌표
    "아스가르드": [(687,532), (952,532), (1217,532),  (687,752), (952,752), (1217,752)],  # 6개 보스 좌표
    "니플하임":   [(687,532), (952,532), (1217,532), (687,752), (952,752)],  # 5개 보스 좌표
    "던전":       [(687,532), (952,532), (1217,532), (687,752), (952,752), (1217,752)],  # 6개 보스 좌표
    "미드가르드": [(687,532), (952,532), (1217,532), (687,752), (952,752)],  # 5개 (니플하임과 동일)
}
